# Copy to .env for local development (streamlit run reads env vars via os.environ,
# so `export` these or use a tool like python-dotenv / direnv locally).
GROQ_API_KEY=your_groq_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
